<?xml version="1.0" encoding="utf-8"?>
<!--
    SOVEREIGN VANTAGE V5.5.92 "ARTHUR EDITION"
    Android Manifest

    © 2025-2026 MiWealth Pty Ltd
    Creator & Founder: Mike Stahl
    Co-Founder & CTO (In Memoriam): Arthur Iain McManus (1966-2025)
    Dedicated to Cathryn 💘
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- ═══ PERMISSIONS ═══ -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:name=".SovereignVantageApp"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SovereignVantage"
        android:networkSecurityConfig="@xml/network_security_config"
        android:usesCleartextTraffic="false">

        <!-- ═══ ACTIVITIES ═══ -->

        <!-- Diagnostic screen (accessible from crash reporter or debug menu) -->
        <activity
            android:name=".DiagnosticActivity"
            android:exported="false"
            android:theme="@android:style/Theme.Material.NoActionBar" />

        <!-- Main Compose entry point — THE LAUNCHER -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.SovereignVantage.Splash"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Backtesting (XML layout) -->
        <activity
            android:name=".ui.BacktestingActivity"
            android:exported="false"
            android:label="Backtest" />

        <!-- Crash Reporter (plain Activity — no AppCompat/Hilt/Compose) -->
        <activity
            android:name=".CrashReporterActivity"
            android:exported="false"
            android:label="Crash Report"
            android:theme="@android:style/Theme.Material.NoActionBar" />

        <!-- Analytics Dashboard (XML layout) -->
        <activity
            android:name=".ui.AnalyticsDashboardActivity"
            android:exported="false"
            android:label="Analytics" />

        <!-- Data Migration (Compose) -->
        <activity
            android:name=".ui.MigrationActivity"
            android:exported="false"
            android:label="Migration" />

        <!-- Widget Configuration (Compose) -->
        <activity
            android:name=".widget.WidgetConfigActivity"
            android:exported="true"
            android:label="Widget Settings">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_CONFIGURE" />
            </intent-filter>
        </activity>

        <!-- ═══ SERVICES ═══ -->

        <!-- Foreground trading service -->
        <service
            android:name=".service.TradingService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!-- ═══ RECEIVERS ═══ -->

        <!-- Restart trading on boot -->
        <receiver
            android:name=".receiver.BootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- ═══ WIDGET PROVIDERS ═══ -->

        <!-- Trading widget -->
        <receiver
            android:name=".widget.TradingWidgetProvider"
            android:exported="true"
            android:label="SV Trading">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/trading_widget_info" />
        </receiver>

        <!-- Portfolio widget -->
        <receiver
            android:name=".widget.PortfolioWidgetProvider"
            android:exported="true"
            android:label="SV Portfolio">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/portfolio_widget_info" />
        </receiver>

        <!-- BUILD #123: FileProvider for sharing system logs -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>
